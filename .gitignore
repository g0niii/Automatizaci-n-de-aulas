# Python
__pycache__/
*.py[cod]
*$py.class
.env
.venv
env/
venv/
ENV/
.DS_Store

# Generado / pesado (no se versiona)
output/
input/
casos/
*.imscc
.claude/
_bib_tmp.py

# 'Elementos de las aulas': SOLO las aulas base se versionan (necesarias para
# generar). El resto (ZIPs de entrega, aulas hechas a mano, catálogos cidiplus)
# es pesado y queda local.
Elementos de las aulas/*
!Elementos de las aulas/_extracted_educacion
!Elementos de las aulas/_extracted_posgrado

# Scratch del proceso SDD / subidas / coverage (no se versiona)
.superpowers/
cursos_subidos/
.coverage
